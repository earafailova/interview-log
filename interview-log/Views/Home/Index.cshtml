@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Interview log</h1>
    <p class="lead">Interview log is the place where you can manage interviews.</p>
    <p><section id="socialLoginForm">
@if (!Request.IsAuthenticated)
{
    @Html.Partial("_ExternalLoginsListPartial", new { Action = "ExternalLogin", ReturnUrl = ViewBag.ReturnUrl })
}
</section></p>
</div>

